---
import MetroPerMillion from "@components/viz/MetroPerMillion"

interface Props {
  caption?: string
  source?: string
}
const base = "/posts/overpopulation/data"
const { caption, source } = Astro.props
---

<figure class="viz-figure not-prose my-10">
  <MetroPerMillion client:visible src={`${base}/metro_per_million.csv`} />
  {
    (caption || source) && (
      <figcaption class="viz-caption">
        {caption && <span>{caption}</span>}
        {source && <span class="viz-source">{source}</span>}
      </figcaption>
    )
  }
</figure>
