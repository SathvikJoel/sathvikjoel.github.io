---
import DensitySlope from "@components/viz/DensitySlope"

interface Props {
  caption?: string
  source?: string
}
const base = "/posts/essays/overpopulation/data"
const { caption, source } = Astro.props
---

<figure class="viz-figure viz-narrow not-prose my-10">
  <DensitySlope client:visible src={`${base}/slope_density_selected.csv`} />
  {
    (caption || source) && (
      <figcaption class="viz-caption">
        {caption && <span>{caption}</span>}
        {source && <span class="viz-source">{source}</span>}
      </figcaption>
    )
  }
</figure>
